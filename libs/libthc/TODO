TODO

General:
  - add draw_ports and draw_port to CairoUtils

IThcWidget:

ModeManager:
  - autoremove destroyed widget
  - implement

SkinManager:

Skin:
  - load_images on demand to reduce memory usage

Slider:
  - two image mode
  - use color

ICloContainer:
  - define the interface
  - list all child
  - pass on_mode_change to all child

CloArea:
  - lock the area size
  - toggle fullscreen
  - resize the area
  - move the area
  - a moved widget doesnt overlap another widget

CloCollapsable:
  - support rename
  - support mode slime

DONE
  - create an helper class witch draw text, etc... using cairo - done [CairoUtils]
  - add CloModeLearn - done
  - add a generic interface to set_value/get_value (port?) -done
  - support CloModeLearn - done
  - move the area - DONE
  - better skin support - DONE
  - maybe a class for parameter (to abstract Adjustment) - DONE
  - split the pure interface and the helper function. (IWidget is abstract)
    => class Widget: public IWidget {};
    => class Slider: public Widget {};
